name: CI

on:
  pull_request:
    branches: [master]

permissions:
  contents: read
  id-token: write
  pull-requests: write

jobs:
  checks:
    uses: ./.github/workflows/_checks.yml
    secrets: inherit
