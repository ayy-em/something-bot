"""Structured logging placeholder.

Cloud Logging picks up plain stdout from Cloud Run, so the production logger
will be wired here in a later issue. Kept as a stub so dependents can already
import ``something_really_bot.logging`` without breaking.
"""
