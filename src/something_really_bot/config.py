"""Configuration loading placeholder.

Real configuration handling (Telegram secret, QA allowlist, BigQuery dataset,
GCS bucket, etc.) is introduced in later issues (#12 onward) once Secret
Manager wiring lands. This module exists now so import paths are stable.
"""
